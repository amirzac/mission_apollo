## Installation via docker:
* `make install`

## Base url:
* Application: `http://mission-apollo.localhost:8000` (requires /etc/hosts changing)

## Run tests:
* `make test` 

## Run project:
* `make up`